diff --git a/Makefile b/Makefile
index 2b41f34f..a4949987 100644
--- a/Makefile
+++ b/Makefile
@@ -7,10 +7,10 @@ all:
 install:
 	@mkdir -p $(DESTDIR)$(PREFIX)/bin
 	@mkdir -p $(DESTDIR)$(MANDIR)/man1
-	@cp -p neofetch $(DESTDIR)$(PREFIX)/bin/neofetch
-	@cp -p neofetch.1 $(DESTDIR)$(MANDIR)/man1
-	@chmod 755 $(DESTDIR)$(PREFIX)/bin/neofetch
+	@cp -p waifetch $(DESTDIR)$(PREFIX)/bin/waifetch
+	@cp -p waifetch.1 $(DESTDIR)$(MANDIR)/man1
+	@chmod 755 $(DESTDIR)$(PREFIX)/bin/waifetch
 
 uninstall:
-	@rm -rf $(DESTDIR)$(PREFIX)/bin/neofetch
-	@rm -rf $(DESTDIR)$(MANDIR)/man1/neofetch.1*
+	@rm -rf $(DESTDIR)$(PREFIX)/bin/waifetch
+	@rm -rf $(DESTDIR)$(MANDIR)/man1/waifetch.1*
diff --git a/neofetch b/waifetch
similarity index 99%
rename from neofetch
rename to waifetch
index 48b96d21..3446ae61 100755
--- a/neofetch
+++ b/waifetch
@@ -754,7 +754,7 @@ disk_display="off"
 #           'viu'
 
 # Flag:     --backend
-image_backend="ascii"
+image_backend="kitty"
 
 # Image Source
 #
@@ -768,7 +768,7 @@ image_backend="ascii"
 # NOTE: 'auto' will pick the best image source for whatever image backend is used.
 #       In ascii mode, distro ascii art will be used and in an image mode, your
 #       wallpaper will be used.
-image_source="auto"
+image_source="waifu"
 
 
 # Ascii Options
@@ -858,7 +858,7 @@ image_loop="off"
 #
 # Default: '~/.cache/thumbnails/neofetch'
 # Values:  'dir'
-thumbnail_dir="${XDG_CACHE_HOME:-${HOME}/.cache}/thumbnails/neofetch"
+thumbnail_dir="${XDG_CACHE_HOME:-${HOME}/.cache}/thumbnails/waifetch"
 
 # Crop mode
 #
@@ -2587,8 +2587,8 @@ get_gpu() {
         ;;
 
         "Mac OS X"|"macOS")
-            if [[ -f "${cache_dir}/neofetch/gpu" ]]; then
-                source "${cache_dir}/neofetch/gpu"
+            if [[ -f "${cache_dir}/waifetch/gpu" ]]; then
+                source "${cache_dir}/waifetch/gpu"
 
             else
                 gpu="$(system_profiler SPDisplaysDataType |\
@@ -4101,26 +4101,41 @@ get_image_source() {
         "auto" | "wall" | "wallpaper")
             get_wallpaper
         ;;
-
+        "waifu")
+            get_waifu
+        ;;
         *)
-            # Get the absolute path.
             image_source="$(get_full_path "$image_source")"
-
             if [[ -d "$image_source" ]]; then
                 shopt -s nullglob
                 files=("${image_source%/}"/*.{png,jpg,jpeg,jpe,gif,svg})
                 shopt -u nullglob
                 image="${files[RANDOM % ${#files[@]}]}"
-
             else
                 image="$image_source"
             fi
         ;;
     esac
-
     err "Image: Using image '$image'"
 }
 
+get_waifu() {
+    # # nsfw
+    # URL_WAIFU=$(curl -s https://api.nekosapi.com/v4/images/random | jq -r '.[0].url')
+    # # safe
+    # curl -s https://api.waifu.pics/sfw/waifu | jq -r '.url'
+    # # safe
+    # URL_WAIFU=$(curl -s https://nekos.best/api/v2/neko | jq -r '.results[0].url')
+    rm -rf "${XDG_CACHE_HOME:-${HOME}/.cache}/thumbnails/waifetch"
+    URL_WAIFU=$(curl -s https://nekos.best/api/v2/neko | jq -r '.results[0].url')
+    EXTENSION_WAIFU=${URL_WAIFU##*.}
+    TMP_PATH_WAIFU_DIR="/tmp/waifetch"
+    mkdir -p "${TMP_PATH_WAIFU_DIR}"
+    TMP_PATH_WAIFU="${TMP_PATH_WAIFU_DIR}/waifetch-image.${EXTENSION_WAIFU}"
+    curl -sL "$URL_WAIFU" -o "$TMP_PATH_WAIFU"
+    image="$TMP_PATH_WAIFU"
+}
+
 get_wallpaper() {
     case $os in
         "Mac OS X"|"macOS")
@@ -4372,7 +4387,7 @@ make_thumbnail() {
     esac
 
     # Create the thumbnail dir if it doesn't exist.
-    mkdir -p "${thumbnail_dir:=${XDG_CACHE_HOME:-${HOME}/.cache}/thumbnails/neofetch}"
+    mkdir -p "${thumbnail_dir:=${XDG_CACHE_HOME:-${HOME}/.cache}/thumbnails/waifetch}"
 
     if [[ ! -f "${thumbnail_dir}/${image_name}" ]]; then
         # Get image size so that we can do a better crop.
@@ -4447,7 +4462,7 @@ display_image() {
                 ueberzug layer --parser bash 0< <(
                     declare -Ap ADD=(\
                         [action]="add"\
-                        [identifier]="neofetch"\
+                        [identifier]="waifetch"\
                         [x]=$xoffset [y]=$yoffset\
                         [path]=$image\
                     )
@@ -4779,19 +4794,19 @@ get_user_config() {
         err "Config: Sourced user config. (${config_file})"
         return
 
-    elif [[ -f "${XDG_CONFIG_HOME}/neofetch/config.conf" ]]; then
-        source "${XDG_CONFIG_HOME}/neofetch/config.conf"
-        err "Config: Sourced user config.    (${XDG_CONFIG_HOME}/neofetch/config.conf)"
+    elif [[ -f "${XDG_CONFIG_HOME}/waifetch/config.conf" ]]; then
+        source "${XDG_CONFIG_HOME}/waifetch/config.conf"
+        err "Config: Sourced user config.    (${XDG_CONFIG_HOME}/waifetch/config.conf)"
 
-    elif [[ -f "${XDG_CONFIG_HOME}/neofetch/config" ]]; then
-        source "${XDG_CONFIG_HOME}/neofetch/config"
-        err "Config: Sourced user config.    (${XDG_CONFIG_HOME}/neofetch/config)"
+    elif [[ -f "${XDG_CONFIG_HOME}/waifetch/config" ]]; then
+        source "${XDG_CONFIG_HOME}/waifetch/config"
+        err "Config: Sourced user config.    (${XDG_CONFIG_HOME}/waifetch/config)"
 
     elif [[ -z "$no_config" ]]; then
-        config_file="${XDG_CONFIG_HOME}/neofetch/config.conf"
+        config_file="${XDG_CONFIG_HOME}/waifetch/config.conf"
 
         # The config file doesn't exist, create it.
-        mkdir -p "${XDG_CONFIG_HOME}/neofetch/"
+        mkdir -p "${XDG_CONFIG_HOME}/waifetch/"
         printf '%s\n' "$config" > "$config_file"
     fi
 }
@@ -4817,8 +4832,8 @@ bar() {
 
 cache() {
     if [[ "$2" ]]; then
-        mkdir -p "${cache_dir}/neofetch"
-        printf "%s" "${1/*-}=\"$2\"" > "${cache_dir}/neofetch/${1/*-}"
+        mkdir -p "${cache_dir}/waifetch"
+        printf "%s" "${1/*-}=\"$2\"" > "${cache_dir}/waifetch/${1/*-}"
     fi
 }
 
@@ -4897,7 +4912,7 @@ cache_uname() {
 
     if [[ "$kernel_name" == "Darwin" ]]; then
         # macOS can report incorrect versions unless this is 0.
-        # https://github.com/dylanaraps/neofetch/issues/1607
+        # https://github.com/dylanaraps/waifetch/issues/1607
         export SYSTEM_VERSION_COMPAT=0
 
         IFS=$'\n' read -d "" -ra sw_vers <<< "$(awk -F'<|>' '/key|string/ {print $3}' \
@@ -4962,7 +4977,7 @@ decode_url() {
 # FINISH UP
 
 usage() { printf "%s" "\
-Usage: neofetch func_name --option \"value\" --option \"value\"
+Usage: waifetch func_name --option \"value\" --option \"value\"
 
 Neofetch is a CLI system information tool written in BASH. Neofetch
 displays information about your system next to an image, your OS logo,
@@ -4976,17 +4991,17 @@ INFO:
     func_name                   Specify a function name (second part of info() from config) to
                                 quickly display only that function's information.
 
-                                Example: neofetch uptime --uptime_shorthand tiny
+                                Example: waifetch uptime --uptime_shorthand tiny
 
-                                Example: neofetch uptime disk wm memory
+                                Example: waifetch uptime disk wm memory
 
                                 This can be used in bars and scripts like so:
 
-                                memory=\"\$(neofetch memory)\"; memory=\"\${memory##*: }\"
+                                memory=\"\$(waifetch memory)\"; memory=\"\${memory##*: }\"
 
                                 For multiple outputs at once (each line of info in an array):
 
-                                IFS=\$'\\n' read -d \"\" -ra info < <(neofetch memory uptime wm)
+                                IFS=\$'\\n' read -d \"\" -ra info < <(waifetch memory uptime wm)
 
                                 info=(\"\${info[@]##*: }\")
 
@@ -4995,7 +5010,7 @@ INFO:
                                 'print_info()' function inside the config file.
                                 For example: 'info \"Memory\" memory' would be '--disable memory'
 
-                                NOTE: You can supply multiple args. eg. 'neofetch --disable cpu gpu'
+                                NOTE: You can supply multiple args. eg. 'waifetch --disable cpu gpu'
 
     --title_fqdn on/off         Hide/Show Fully Qualified Domain Name in title.
     --package_managers on/off   Hide/Show Package Manager names . (on, tiny, off)
@@ -5114,7 +5129,7 @@ IMAGE BACKEND:
 
     --ascii source              Shortcut to use 'ascii' backend.
 
-                                NEW: neofetch --ascii \"\$(fortune | cowsay -W 30)\"
+                                NEW: waifetch --ascii \"\$(fortune | cowsay -W 30)\"
 
     --caca source               Shortcut to use 'caca' backend.
     --catimg source             Shortcut to use 'catimg' backend.
@@ -5222,7 +5237,7 @@ OTHER:
     --print_config              Print the default config file to stdout.
     --stdout                    Turn off all colors and disables any ASCII/image backend.
     --help                      Print this text and exit
-    --version                   Show neofetch version
+    --version                   Show waifetch version
     -v                          Display error messages.
     -vv                         Display a verbose log for error reporting.
 
@@ -5230,7 +5245,7 @@ DEVELOPER:
     --gen-man                   Generate a manpage for Neofetch in your PWD. (Requires GNU help2man)
 
 
-Report bugs to https://github.com/dylanaraps/neofetch/issues
+Report bugs to https://github.com/dylanaraps/waifetch/issues
 
 "
 exit 1
@@ -5379,8 +5394,9 @@ get_args() {
             "--gap") gap="$2" ;;
             "--clean")
                 [[ -d "$thumbnail_dir" ]] && rm -rf "$thumbnail_dir"
-                rm -rf "/Library/Caches/neofetch/"
-                rm -rf "/tmp/neofetch/"
+                rm -rf "/Library/Caches/waifetch/"
+                rm -rf "/tmp/waifetch/"
+                rm -rf "${XDG_CACHE_HOME:-${HOME}/.cache}/thumbnails/waifetch"
                 exit
             ;;
 
@@ -5428,7 +5444,7 @@ get_args() {
             ;;
             "--gen-man")
                 help2man -n "A fast, highly customizable system info script" \
-                          -N ./neofetch -o neofetch.1
+                          -N ./waifetch -o waifetch.1
                 exit 1
             ;;
 
@@ -11574,7 +11590,7 @@ main() {
     [[ $image_backend == *w3m* ]] && display_image
     [[ $image_backend == *ueberzug* ]] && display_image
 
-    # Add neofetch info to verbose output.
+    # Add waifetch info to verbose output.
     err "Neofetch command: $0 $*"
     err "Neofetch version: $version"
 
diff --git a/neofetch.1 b/waifetch.1
similarity index 96%
rename from neofetch.1
rename to waifetch.1
index b90b5781..5e672bfa 100644
--- a/neofetch.1
+++ b/waifetch.1
@@ -3,7 +3,7 @@
 .SH NAME
 Neofetch \- A fast, highly customizable system info script
 .SH SYNOPSIS
-.B neofetch
+.B waifetch
 \fI\,func_name --option "value" --option "value"\/\fR
 .SH DESCRIPTION
 Neofetch is a CLI system information tool written in BASH. Neofetch
@@ -18,17 +18,17 @@ func_name
 Specify a function name (second part of info() from config) to
 quickly display only that function's information.
 .IP
-Example: neofetch uptime \fB\-\-uptime_shorthand\fR tiny
+Example: waifetch uptime \fB\-\-uptime_shorthand\fR tiny
 .IP
-Example: neofetch uptime disk wm memory
+Example: waifetch uptime disk wm memory
 .IP
 This can be used in bars and scripts like so:
 .IP
-memory="$(neofetch memory)"; memory="${memory##*: }"
+memory="$(waifetch memory)"; memory="${memory##*: }"
 .IP
 For multiple outputs at once (each line of info in an array):
 .IP
-IFS=$'\en' read \fB\-d\fR "" \fB\-ra\fR info < <(neofetch memory uptime wm)
+IFS=$'\en' read \fB\-d\fR "" \fB\-ra\fR info < <(waifetch memory uptime wm)
 .IP
 info=("${info[@]##*: }")
 .TP
@@ -38,7 +38,7 @@ in the output. 'infoname' is the function name from the
 \&'print_info()' function inside the config file.
 For example: 'info "Memory" memory' would be '\-\-disable memory'
 .IP
-NOTE: You can supply multiple args. eg. 'neofetch \fB\-\-disable\fR cpu gpu'
+NOTE: You can supply multiple args. eg. 'waifetch \fB\-\-disable\fR cpu gpu'
 .TP
 \fB\-\-title_fqdn\fR on/off
 Hide/Show Fully Qualified Domain Name in title.
@@ -244,7 +244,7 @@ Possible values: 'auto', 'ascii', 'wallpaper', '/path/to/img',
 \fB\-\-ascii\fR source
 Shortcut to use 'ascii' backend.
 .IP
-NEW: neofetch \fB\-\-ascii\fR "$(fortune | cowsay \fB\-W\fR 30)"
+NEW: waifetch \fB\-\-ascii\fR "$(fortune | cowsay \fB\-W\fR 30)"
 .TP
 \fB\-\-caca\fR source
 Shortcut to use 'caca' backend.
@@ -416,7 +416,7 @@ Turn off all colors and disables any ASCII/image backend.
 Print this text and exit
 .TP
 \fB\-\-version\fR
-Show neofetch version
+Show waifetch version
 .TP
 \fB\-v\fR
 Display error messages.
@@ -428,4 +428,4 @@ Display a verbose log for error reporting.
 \fB\-\-gen\-man\fR
 Generate a manpage for Neofetch in your PWD. (Requires GNU help2man)
 .SH "REPORTING BUGS"
-Report bugs to https://github.com/dylanaraps/neofetch/issues
+Report bugs to https://github.com/dylanaraps/waifetch/issues
